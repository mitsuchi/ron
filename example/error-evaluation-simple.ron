# エラーケース5: 評価時のエラー（mainブロックで条件が満たせない）

op 100 : _ + _
op 50 : _ => _

syntax {
    t ::= n | t + t
    n ::= 0 | 1 | 2 | 3
}

# 足し算のルール（3 + は定義していない）
0 + 1 => 1
1 + 1 => 2
1 + 2 => 3
2 + 1 => 3
2 + 2 => 4

main {
    # エラー: 3 + 1 のルールが定義されていない
    3 + 1 => x
}

