# エラーケース6: 矢印が欠けている（構文的には正しいが、型が合わない）

op 50 : λ _ : _ . _
op 40 r : _ -> _
op 30 : _ : _
op 20 : _ , _
op 10 : _ ⊢ _ : _

syntax {
    t ::= x | λ x : T . t
    x ::= a | b | c
    T ::= Bool | T -> T
}

# T-Abs
Γ ⊢ (λ x : T1 . t) : T1 -> T2 {
    Γ, x : T1 ⊢ t : T2
}

# T-Var
Γ ⊢ x : T {
    x : T
}

main {
    # エラー: Bool -> の後ろが欠けている
    φ, a : Bool ⊢ λ a : Bool . a : Bool ->
}

