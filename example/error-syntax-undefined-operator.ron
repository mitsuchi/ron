# エラーケース2: syntaxブロック内で未定義の演算子を使用

op 100 : _ + _
op 50 : if _ then _ else _

syntax {
    t ::= n | t + t | if t then t else t | t && t  # エラー: && 演算子が未定義
    n ::= 0 | 1 | 2
}

0 + 1 => 1
1 + 1 => 2

main {
    1 + 1 => x
}

